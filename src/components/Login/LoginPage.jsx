import styled from "styled-components";

export default function LoginPage() {
  return (
    <PageContainer>
      <p>Login</p>
    </PageContainer>
  );
}

const PageContainer = styled.div`
  font-size: 24px;
`;
